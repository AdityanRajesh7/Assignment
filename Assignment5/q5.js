function findLocateIndex() {
    const str = "Please locate where 'locate' occurs!";
    const position = str.indexOf("locate");
    
    console.log(`The word 'locate' first appears at position: ${position}`);
}

findLocateIndex();